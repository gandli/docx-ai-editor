import React from 'react';

const RuleSource = ({ source }) => {
  return (
    <div className="rule-source">
      <strong>Rule Source:</strong> {source}
    </div>
  );
};

export default RuleSource;